name: deep-pose-tracker

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.13.5
  - numpy=1.24.4
  - pandas
  - scipy
  - matplotlib
  - pip

  - pip:
      - ultralytics==8.3.230
      - opencv-python
      - motmetrics==1.4.0
